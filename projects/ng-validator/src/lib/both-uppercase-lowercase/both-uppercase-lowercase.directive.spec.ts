import { BothUppercaseLowercaseDirective } from './both-uppercase-lowercase.directive';

describe('BothUppercaseLowercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new BothUppercaseLowercaseDirective();
    expect(directive).toBeTruthy();
  });
});
